// lista e implementação de cada upgrade

var valorClick = 1;

function comprarUpgrade(idUpgrade) {
    switch(idUpgrade) {
        case 0:
            // caso seja o primeiro upgrade
            valorClick += 1;
            break;
    }
}